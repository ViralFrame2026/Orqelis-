import Link from "next/link";
import { ArrowRight, Boxes, ChartNoAxesCombined, Eye, MessageSquareText, Plus, Settings, Tags } from "lucide-react";
import { getAllProductsForAdmin, getCategories } from "@/services/catalog";

export default async function AdminDashboardPage() {
  const [products, categories] = await Promise.all([getAllProductsForAdmin(), getCategories()]);
  const visible = products.filter((product) => product.status !== "hidden").length;
  const cards = [
    { label: "Productos", value: products.length, detail: `${visible} visibles`, icon: Boxes, href: "/admin/productos", color: "bg-sage text-forest" },
    { label: "Categorías", value: categories.length, detail: "Catálogo organizado", icon: Tags, href: "/admin/categorias", color: "bg-sand/70 text-clay-dark" },
    { label: "Consultas", value: "—", detail: "Próxima fase", icon: MessageSquareText, href: "/admin/consultas", color: "bg-[#e6e0f1] text-[#6c5685]" },
    { label: "Ventas", value: "—", detail: "Próxima fase", icon: ChartNoAxesCombined, href: "/admin/ventas", color: "bg-[#dce8ee] text-[#416477]" },
  ];
  return <div><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-black uppercase tracking-[.16em] text-clay">Vista general</p><h1 className="font-display mt-2 text-4xl">Buen día 👋</h1><p className="mt-2 text-sm text-ink/50">Así está tu tienda en este momento.</p></div><Link href="/admin/productos/nuevo" className="focus-ring inline-flex min-h-11 items-center gap-2 rounded-xl bg-ink px-5 text-sm font-extrabold text-white"><Plus size={17} /> Nuevo producto</Link></div><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(({ label, value, detail, icon: Icon, href, color }) => <Link key={label} href={href} className="focus-ring group rounded-[1.4rem] border border-ink/8 bg-white p-5 transition hover:-translate-y-0.5 hover:shadow-lg"><div className="flex items-start justify-between"><span className={`grid size-11 place-items-center rounded-xl ${color}`}><Icon size={20} /></span><ArrowRight size={17} className="text-ink/24 transition group-hover:translate-x-1 group-hover:text-clay" /></div><p className="mt-6 text-3xl font-black tracking-tight">{value}</p><p className="mt-1 text-sm font-extrabold">{label}</p><p className="mt-1 text-xs text-ink/42">{detail}</p></Link>)}</div><div className="mt-7 grid gap-5 xl:grid-cols-[1.4fr_.6fr]"><section className="rounded-[1.4rem] border border-ink/8 bg-white p-5 sm:p-7"><div className="flex items-center justify-between"><h2 className="font-display text-2xl">Productos recientes</h2><Link href="/admin/productos" className="text-xs font-extrabold text-clay">Ver todos</Link></div><div className="mt-5 grid gap-2">{products.slice(0, 5).map((product) => <Link key={product.id} href={`/admin/productos/${product.id}/editar`} className="flex items-center gap-3 rounded-xl bg-[#f8f6f2] p-3"><span className="grid size-10 place-items-center rounded-lg bg-white"><Boxes size={17} /></span><div className="min-w-0 flex-1"><p className="truncate text-sm font-extrabold">{product.name}</p><p className="text-xs text-ink/42">{product.category.name}</p></div><span className={`rounded-full px-2 py-1 text-[.6rem] font-black uppercase ${product.status === "hidden" ? "bg-ink/8 text-ink/45" : "bg-sage text-forest"}`}>{product.status === "hidden" ? "Oculto" : "Visible"}</span></Link>)}{!products.length ? <p className="py-10 text-center text-sm text-ink/40">Todavía no cargaste productos.</p> : null}</div></section><section className="rounded-[1.4rem] bg-forest p-6 text-white"><Settings className="text-sand" /><h2 className="font-display mt-5 text-3xl">Antes de publicar</h2><p className="mt-3 text-sm leading-6 text-white/58">Completá el nombre de la tienda, WhatsApp e Instagram para activar todos los enlaces.</p><Link href="/admin/configuracion" className="focus-ring mt-6 inline-flex min-h-11 items-center gap-2 rounded-xl bg-white px-4 text-sm font-extrabold text-ink">Configurar tienda <ArrowRight size={16} /></Link><div className="mt-7 flex items-center gap-2 border-t border-white/10 pt-5 text-xs text-white/48"><Eye size={15} /> Revisá la tienda pública al terminar.</div></section></div></div>;
}
